const liara = new Path2D();
liara.ellipse(385, 367, 100, 140, 0, 0, 2 * Math.PI);

const talia = new Path2D();
talia.ellipse(315, 612, 45, 75, 0, 0, 2 * Math.PI);

const garrus = new Path2D();
garrus.ellipse(720, 180, 95, 155, 90, 0, 2 * Math.PI);

export { liara, talia, garrus };